# Scholar X

一个完全基于 Figma 设计的学术出版物平台，使用 Next.js 14、Supabase 和 Tailwind CSS 构建。

## ✨ 特点

- 🎨 **完美还原 Figma 设计** - 100% 遵循原始设计稿
- 🌙 **深色主题** - 专业的深色界面，适合长时间阅读
- 📱 **响应式设计** - 支持桌面和移动设备
- ⚡ **Next.js 14** - 使用最新的 App Router
- 🎯 **TypeScript** - 完整的类型安全
- 🎭 **Tailwind CSS** - 现代化的样式系统

## 技术栈

- **框架**: Next.js 14 (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS
- **图标**: Lucide React
- **字体**: Inter + Instrument Serif (Google Fonts)
- **数据库**: Supabase
- **动画**: Framer Motion
- **部署**: Vercel

## 🚀 开始使用

### 1. 修复 npm 权限问题（如果需要）

```bash
sudo chown -R 501:20 "/Users/karl4chill/.npm"
```

### 2. 安装依赖

```bash
npm install
# 或
yarn install
# 或
pnpm install
```

### 3. 配置环境变量

创建 `.env.local` 文件并添加以下内容：

```env
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

### 4. 启动开发服务器

```bash
npm run dev
# 或
yarn dev
# 或
pnpm dev
```

在浏览器中打开 [http://localhost:3000](http://localhost:3000) 查看结果。

## 📁 项目结构

```
scox/
├── app/                      # Next.js 应用目录
│   ├── layout.tsx           # 根布局（包含字体配置）
│   ├── page.tsx             # 主页面（出版物列表）
│   └── globals.css          # 全局样式
├── components/              # React 组件
│   ├── Sidebar.tsx         # 左侧导航栏
│   ├── TopBar.tsx          # 顶部栏（搜索、按钮）
│   ├── PublicationCard.tsx # 出版物卡片
│   ├── RightSidebar.tsx    # 右侧边栏（最新提交）
│   ├── Pagination.tsx      # 分页组件
│   └── Footer.tsx          # 页脚
├── lib/                     # 工具库
│   └── supabase/           # Supabase 配置
│       ├── client.ts       # 客户端
│       ├── server.ts       # 服务端
│       └── middleware.ts   # 中间件
├── public/                 # 静态资源
│   ├── avatar.svg         # 头像占位符
│   └── assets/            # 图片资源
├── package.json           # 依赖配置
├── tsconfig.json          # TypeScript 配置
├── tailwind.config.js     # Tailwind 配置
└── next.config.js         # Next.js 配置
```

## Supabase 设置

1. 在 [Supabase](https://supabase.com) 创建一个新项目
2. 复制项目的 URL 和 anon key
3. 将它们添加到 `.env.local` 文件中

## 部署到 Vercel

1. 将代码推送到 GitHub
2. 在 [Vercel](https://vercel.com) 导入你的仓库
3. 配置环境变量
4. 点击部署

## 🎨 设计系统

### 颜色方案
- **背景色**: `#111116`
- **TrustBlue**: 用于主要操作和链接
  - TrustBlue/0: `#ffffff`
  - TrustBlue/150: `#bed0f9`
  - TrustBlue/350: `#2563eb`
- **OpenGreen**: 用于成功状态
  - OpenGreen/300: `#3dc799`
  - OpenGreen/600: `#064a34`

### 字体
- **Inter**: 用于界面文本和正文
- **Instrument Serif**: 用于标题和引用

## 🎯 页面组成

### 主要区域
1. **左侧导航栏** (72px 宽) - 固定在左侧，包含主要导航图标
2. **顶部栏** (72px 高) - 搜索框、通知、创建按钮
3. **主内容区** - 出版物网格（3列布局）
   - 特色出版物（顶部大卡片）
   - 常规出版物网格
   - 分页控件
4. **右侧边栏** (386px 宽) - 显示最新提交和待审核内容
5. **页脚** - 链接和社交媒体图标

### 组件特性
- 🏷️ **标签系统**: preserved、raising、peer reviewed
- 👤 **作者信息**: 头像 + 姓名
- 📄 **出版物卡片**: 标题、摘要、作者、标签
- 🔍 **搜索功能**: 顶部搜索栏
- 📑 **分页**: 底部页码导航

## ✨ 功能特性

- ✅ 完全遵循 Figma 设计
- ✅ 响应式布局（自适应更宽视口）
- ✅ TypeScript 类型安全
- ✅ Supabase 集成（认证和数据库）
- ✅ Tailwind CSS 样式系统
- ✅ Lucide React 图标
- ✅ Next.js 14 App Router
- ✅ 优化的性能和 SEO
- ✅ 自定义滚动条样式

## 开发指南

- 组件使用 TypeScript 编写
- 遵循 Next.js 13+ App Router 最佳实践
- 使用 Tailwind CSS 进行样式设计
- 使用 Framer Motion 添加动画效果
- 通过 Supabase 管理数据和认证

## 许可证

MIT

